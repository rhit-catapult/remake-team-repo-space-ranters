# PyGameProject
Project Starter Code and References

Quick setup (Windows PowerShell):

```powershell
# Recreate a .venv using the newest Python installed and install deps
.\scripts\recreate_venv.ps1

# Activate the venv
.\.venv\Scripts\Activate.ps1

# Run the project
python project.py
```
