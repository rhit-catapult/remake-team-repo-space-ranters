[project]
name = "remake-team-repo-space-ranters"
version = "0.1.0"
description = "A small pygame project and RTS codefolder"
requires-python = ">=3.14"
dependencies = [
    "pygame-ce>=2.5.7"
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
